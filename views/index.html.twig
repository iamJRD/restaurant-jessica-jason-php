<!DOCTYPE html>
<html>
    <head>
        <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.6/css/bootstrap.min.css" integrity="sha384-1q8mTJOASx8j1Au+a5WDVnPi2lkFfwwEAa8hDDdjZlpLegxhjVME1fgjWPGmkzs7" crossorigin="anonymous">
        <meta charset="utf-8">
        <title>Create a Cuisine</title>
    </head>
    <body>
        <div class="container">
            <h3>Add a cuisine!</h3>
            <form class="form-group" action="/add_cuisine" method="post">
                <label for="name">Name of Cuisine</label>
                <input type="text" name="name" value="">
                <button type="submit" class="btn btn-default" name="button">Submit</button>
            </form>
            <h5>Current List of Cuisines:</h5>
            <ul>
            {% for cuisine in cuisines %}
                <li><a href="/cuisine/{{ cuisine.getId }}">{{ cuisine.getName }}</a></li>
            {% endfor %}
            </ul>

            <div class="row">
                <div class="col-md-12">
                    <form action="/delete_cuisines" method="post">
                        <button type="submit" class="btn btn-info" name="button">Clear Cuisines</button>
                    </form>
                    <br>
                    <br>
                    <br>
                    <form action="/delete_restaurants" method="post">
                        <button type="submit" class="btn btn-danger" name="button">Clear ALL Restaurants</button>
                    </form>
                </div>
            </div>
        </div>
    </body>
</html>
